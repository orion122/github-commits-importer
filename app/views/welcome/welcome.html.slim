main class='container'
  .jumbotron
    h1 Github Commits Importer

    = form_with(url: 'import', local: true, method: 'post') do
      .form-row.align-items-center
        .col-sm-3.my-1
          = text_field_tag(:owner, 'orion122', class: 'form-control', placeholder: 'Owner')

        .col-sm-3.my-1
          = text_field_tag(:repo, 'BotBoobot', class: 'form-control', placeholder: 'Repo')

        .col-sm-3.my-1
          = text_field_tag(:author_email, nil, type: :email, class: 'form-control', placeholder: 'Email')

        .col-auto.my-1
          = button_tag 'Import commits', class: 'btn btn-primary'

  - flash.each do |type, message|
    .alert class=bootstrap_class_for(type)
      = message